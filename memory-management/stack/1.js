// Stack

function main() {
  const numVal = 1;
  const aFlag = true;
}

/**
 * The above two variables will be stored in a stack frame as they are local to the function
 * The frame holds any local variables, arguments to the function and return values
 */
